# === Video Analysis Service (TensorFlow 기반) ===

# === TensorFlow Ecosystem (CPU 전용으로 최적화) ===
tensorflow-cpu==2.15.0  # CPU 전용 (메모리 절약)

# === Computer Vision & MediaPipe ===
opencv-python==4.8.1.78
mediapipe==0.10.8
deepface==0.0.79

# === Image Processing ===
Pillow==10.1.0
numpy==1.24.3
scipy==1.11.4

# === Video Processing ===
moviepy==1.0.3
ffmpeg-python==0.2.0

# === FastAPI & HTTP ===
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.6
httpx==0.27.0

# === Utilities (최소화) ===
python-dotenv==1.0.1
pydantic==2.9.2
requests==2.31.0

# === Database (최소화) ===
redis==5.0.1

# === Audio Processing (Whisper for video audio) ===
openai-whisper==20231117
librosa==0.10.1  # 버전 고정으로 메모리 절약
soundfile==0.12.1  # 버전 고정으로 메모리 절약

# === Monitoring & Logging ===
psutil==5.9.5 