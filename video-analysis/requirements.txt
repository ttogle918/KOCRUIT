# === Video Analysis Service (TensorFlow 기반) ===

# === TensorFlow Ecosystem (CPU 전용으로 최적화) ===
tensorflow-cpu==2.15.0  # CPU 전용 (메모리 절약)
tensorflow-hub==0.15.0

# === Computer Vision & MediaPipe ===
opencv-python==4.8.1.78
mediapipe==0.10.7
deepface==0.0.79  # TensorFlow 기반 감정 분석

# === Image Processing ===
Pillow==10.1.0
numpy==1.24.3
scipy==1.11.4

# === Video Processing ===
moviepy==1.0.3
ffmpeg-python==0.2.0

# === FastAPI & HTTP ===
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
httpx==0.27.0  # Agent 서비스 연동용

# === Utilities (최소화) ===
python-dotenv==1.0.1
pydantic==2.9.2
requests==2.31.0

# === Database (최소화) ===
redis==5.0.1

# === Audio Processing (기본 분석만) ===
librosa==0.10.1  # 버전 고정으로 메모리 절약
soundfile==0.12.1  # 버전 고정으로 메모리 절약
# Whisper와 pyannote.audio는 agent 서비스에서 처리

# === Monitoring & Logging ===
psutil==5.9.6 
google-api-python-client==2.108.0
google-auth-httplib2==0.1.1 